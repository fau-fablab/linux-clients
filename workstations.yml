---

- name: workstation deployment
  hosts: workstations
  vars_files:
    - group_vars/all.yml
    - 'group_vars/{{ ansible_distribution }}.yml'
  roles:
    - role: users
      tags: users
    - role: common
      tags: common
    - role: fablab-share
      tags: fablab-share
